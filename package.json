{
  "name": "jey-portfolio-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix JEY run dev -- --host 0.0.0.0",
    "build": "npm --prefix JEY run build",
    "preview": "npm --prefix JEY run preview -- --host 0.0.0.0",
    "start": "npm --prefix JEY run dev -- --host 0.0.0.0"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1"
  }
}
